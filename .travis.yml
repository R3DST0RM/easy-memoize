language: node.js

before_script:
  - npm install
  - npm run lint

branches:
  only:
    - master

cache:
  directories:
    - node_modules

script:
  - npm run test
  - npm run coveralls
  - npm run build